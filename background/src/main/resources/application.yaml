spring:
  datasource:
    url: jdbc:mysql://localhost:3306/license_db
    username: root
    password: my-secret-pw
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect